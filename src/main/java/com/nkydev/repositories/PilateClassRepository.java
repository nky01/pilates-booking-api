package com.nkydev.repositories;

import com.nkydev.entities.PilateClass;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PilateClassRepository extends JpaRepository<PilateClass, Long> {
}
